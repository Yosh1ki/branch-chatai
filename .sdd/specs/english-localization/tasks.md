# 実装タスクリスト

## セクション1：データモデル実装
- [x] 1.1 必要な型定義・データ構造を作成する（目安: 1-2時間）
  - `src/lib/i18n/types.ts` に `LocaleCode`、`TranslationKey`、`TranslationCatalog`、`LocalizedErrorPayload` を定義する
  - `ja`/`en` のキー集合が一致することを型レベルで検証できる構造を実装する
  - 設計書「データモデル」の各モデルを実装する
- [x] 1.2 データ永続化層を実装する（目安: 1-2時間）
  - DB変更は行わず、`src/lib/i18n/dictionaries/ja.ts` と `src/lib/i18n/dictionaries/en.ts` を静的ストレージとして実装する
  - `src/lib/i18n/index.ts` に辞書の取得・フォールバック・メッセージ解決の読み出し処理（CRUD相当の Read）を実装する
  - 将来のユーザー単位ロケール永続化に備え、`locale` 解決APIの入出力を拡張しやすい形に整理する

## セクション2：ビジネスロジック実装
- [x] 2.1 Localization Catalog のコア処理を実装する（目安: 2-3時間）
  - `t(key, locale)` と埋め込み変数展開処理を実装する
  - design.md の処理フロー 1-3（ロケール決定、辞書解決、UI操作時メッセージ表示）に対応する
  - キー未定義時の既定ロケールフォールバックを実装する
- [x] 2.2 Locale Resolver と Localized UI Adapter の処理を実装する（目安: 2-3時間）
  - `src/lib/i18n/locale.ts` で `Accept-Language`、クエリ、クッキーからロケールを決定する
  - `src/app/layout.tsx` で `lang` 属性とロケール配布を実装する
  - design.md の処理フロー 4-5（API返却メッセージ統一、未定義検知）に対応する
- [x] 2.3 エラーハンドリングを実装する（目安: 1-2時間）
  - `src/lib/i18n/error-messages.ts` を作成し、エラーコードと翻訳キーを対応付ける
  - エラーケース1（翻訳キー未定義）とエラーケース2（不正ロケール）をフォールバック処理で実装する
  - エラーケース3（API文言残存）をコード経由表示へ移行し、エラーケース4（置換漏れ）の検知用チェック項目を整備する

## セクション3：インターフェース実装
- [x] 3.1 UIコンポーネント/APIエンドポイントを作成する（目安: 2-3時間）
  - `src/app/login/page.tsx`、`src/app/settings/page.tsx`、`src/app/chats/page.tsx` の主要文言を辞書キー参照へ置換する
  - `src/components/ChatCanvasShell.tsx`、`src/components/AssistantCard.tsx`、`src/components/chats/*`、`src/components/UserBubble.tsx`、`src/components/theme/theme-toggle.tsx` の表示文言を置換する
  - APIユーザー向けエラー文言（例: 日次上限、送信失敗）を `LocalizedErrorPayload` 形式へ統一する
- [x] 3.2 入力バリデーションを実装する（目安: 1-2時間）
  - ロケール入力値を `ja`/`en` のみ許可し、未対応値は既定ロケールへフォールバックする
  - `t` 関数のキー入力を型で制約し、不正キー参照をビルド時に検出できるようにする
  - 必須埋め込み変数が不足する場合の安全なフォールバックを実装する
- [x] 3.3 出力フォーマットを実装する（目安: 1-2時間）
  - UI表示は常にローカライズ済み文字列を返すようにする
  - APIエラーは `code` と `message` を含む統一形式で返却する
  - 既存表示（分岐、モデル選択、プラン表示）の視認性を保つテキスト長・改行を調整する

## セクション4：統合とテスト
- [x] 4.1 コンポーネントを統合する（目安: 1-2時間）
  - `layout` から各画面・コンポーネントへロケールが伝播することを確認する
  - チャット、ログイン、設定で辞書キー解決とエラー文言解決が同じ経路になるよう統合する
  - 既存の認証・課金・チャット送受信ロジックに副作用がないことを確認する
- [x] 4.2 基本的な動作テストを実装する（目安: 2-3時間）
  - ユニットテスト：`t` 関数、ロケール解決、フォールバック、エラーキー解決を追加する
  - 統合テスト：主要画面（チャット/ログイン/設定）で英語文言表示を確認する
  - APIテスト：代表的エラー時に英語 `message` が返ることを確認する
- [x] 4.3 要件の受入基準を満たすことを確認する（目安: 1-2時間）
  - 要件1：主要UI文言（ボタン、見出し、ナビ、分岐、ミニマップ、モデル選択）が英語表示されることを確認する
  - 要件2：主要エラー/警告/通知と日次上限メッセージが英語表示されることを確認する
  - 要件3：英語文言が `src/lib/i18n` に一元管理され、追跡更新可能であることを確認する
