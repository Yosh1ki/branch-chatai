# TODO

## P1
- [ ] 法務ページを公開する（`/privacy`、`/terms`、`/commercial-transactions`、`/external-transmission`）
- [ ] 法務文書バージョン管理を実装する（`LegalDocumentVersion` テーブル + 有効版取得API）
- [ ] 同意ログを実装する（`PolicyConsentLog` テーブル + `POST /api/legal/consents`）
- [ ] 同意UIを実装する（規約/プライバシーポリシー同意を必須化）
- [ ] ユーザー権利対応フォーム/APIを実装する（開示/訂正/削除/利用停止）
- [ ] 請求管理テーブルを実装する（`PrivacyRequest` テーブル + ステータス管理）
- [ ] アカウント削除フローを実装する（UI + `POST /api/account/delete` + 非同期削除ジョブ）
- [ ] 外部送信情報ページを実装する（送信先/送信項目/利用目的の明示）
- [ ] 法務関連のi18n文言を追加する（`ja`/`en` 辞書）
- [ ] 法務導線のE2Eテストを追加する（法務ページ閲覧、同意必須、同意保存、請求送信、削除）
- [ ] stripe系のapiとchat送信のapiをsupabaseに置く
- [✖︎] 本番DBの作成
- [✖︎] DBのセキュリティポリシーの作成

### 機能やUI
- [ ] web検索を出す条件をもっと緩く
- [ ] ファイルをインプットできるように
- [ ] UIをもっとリッチにしてAI感なくす
- [ ] ブランチピル押された時に入力カードにフォーカスを当てるように
- [ ] loginページのデモアニメーション作成
- [ ] loginページのUI
- [✖︎] 子ブランチの縦幅を固定の高さにして超えたらスクロールするようにする
- [✖︎] ブランチの数でインジケーターを表示するのではなく、列で判断する
- [✖︎] ブランチが切られる時のアニメーション
- [✖︎] 設定画面の各項目の説明文消す
- [✖︎] ライトモード時のchatカードの背景色少し濃くして見やすくする
- [✖︎] ダークモードの実装
- [✖︎] 英語対応

## P2
- [✖︎] Pro課金（Stripe）の実装を完了する（購読導線/API/Webhook/状態反映）。
- [✖︎] 日次上限リセット時刻を仕様で明確化し、実装をタイムゾーン固定にする（現状はサーバーローカル時刻依存）。
- [ ] メール配信同意管理を実装する（オプトイン、配信停止リンク、停止反映）
- [ ] インシデント対応Runbookを整備する（漏えい時の速報/確報フローと監査ログ抽出手順）

## P3
- [✖︎] `.js` と `.ts` の重複実装を整理し、TypeScriptへ統一する（例: `conversation-history`, `usage-limiter`）。
- [ ] テスト実行時の `MODULE_TYPELESS_PACKAGE_JSON` 警告を解消する。
- [ ] `baseline-browser-mapping` の警告を解消する（依存更新）。

##　将来対応
- [ ] ミニマップ機能を実装する（現在は未実装）。
- [ ] メッセージ折りたたみ機能を実装する（`isCollapsed` のUI反映）。
- [ ] 折りたたみ時に表示する inline title を自動生成・保存・表示する（`autoTitle` の実利用）。
- [ ] ブランチ作成APIを仕様準拠にする（`POST /api/messages/{id}/branch` を追加し、`/api/chat` の暗黙作成は移行期間は互換維持）。
- [ ] 文章や単語を選択してボタンを押したらすぐその意味や言葉を教えてくれる
- [ ] Chatの中身を分析して一枚にまとめてくれる機能